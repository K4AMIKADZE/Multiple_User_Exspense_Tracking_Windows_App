this program need to have 
https://gluonhq.com/products/javafx/
(javafx-sdk-17)
and
put the url into .vscode

Program uses mysql to store info automtically, uses AES encryption each user gets its own encryption key, keys are stored in the database not secure way but due to complecity this is the best way for this particular project, app has admin block where there are information about every user and their combined exspenses charts tables, message block, for admin terminal, settings
